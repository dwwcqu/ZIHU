# C++ 前向声明

## 问题：在 C++ 中，前向声明(Forward Declaration)的类型可以怎么使用？

描述：

在 C++ 中，我们可以通过一个前向声明，声明一个在外部定义的类型，那么在这个前向声明的文件中，要如何使用这个声明的类型？能用这个声明类型做那些操作？下图中列举了一个类型可能会被使用的场景，那些场景是可以正确使用的？那些是不能使用的？如果要使用模板类型的前向声明，又有那些使用场景？以及那些限制？